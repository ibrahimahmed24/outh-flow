version: "3.8"
services:
  app: 
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=development
    command: npm run dev
    ports:
      - "3000:3000"
  